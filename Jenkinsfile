pipeline {
  agent any
  stages {
    stage('checkout') {
      steps {
        git(url: 'git@github.com:gjagadish473/pipeline-one.git', branch: 'main')
      }
    }

    stage('build') {
      steps {
        sh 'ls'
      }
    }

    stage('commit') {
      steps {
        sh 'ls'
      }
    }

    stage('deploy') {
      steps {
        sh 'ls'
      }
    }

  }
}